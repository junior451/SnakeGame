This is a simple implementation of the classic snake game using c++ (command line version)

You can run and play the game locally by installing the c++ and lncurses library and running
### g++ main.cpp -lncurses
### ./a.out
